---
title: <QuestionModal />
description: Usage of the question modal component
sidebar_position: 7
tags: [components, modal]
---

# \<QuestionModal />

## Component props
<AssignableProps
  data={[
    {name: 'cancel', value: '() => void', required: true, definition: 'Function that executes whenever a user cancels'},
    {name: 'cancelLabel', value: 'string', required: true, definition: 'Label for the cancel button'},
    {name: 'confirm', value: '() => void', required: true, definition: 'Function that executes whenever a user confirms'},
    {name: 'confirmLabel', value: 'string', required: true, definition: 'Label for the confirm button'},
    {name: 'message', value: 'string | []string', required: true, definition: 'Modal message'},
    {name: 'title', value: 'string', required: true, definition: 'Modal title'},
  ]}
/>

## Example usage
<QuestionExample/>

&NewLine;

```tsx
<QuestionModal
  title={'Question title'}
  message={['Confirm to display an alert', 'cancel to close this modal']}
  cancel={() => setModalVis(false)}
  cancelLabel={'cancel'}
  confirm={() => alert('Hello!')}
  confirmLabel={'confirm'}
/>
```

## Playground
```tsx live
function Playground() {
  const [modalVis, setModalVis] = useState(false);

  function confirmModal() {
    setModalVis(false);

    alert('Modal confirmed!');
  }

  return (
    <>
      <CustomButton label={'show Modal'} onClick={() => setModalVis(true)} />

      {modalVis &&
        <QuestionModal
          title={'Modal title'}
          message={'Modal message'}
          cancel={() => setModalVis(false)}
          cancelLabel={'cancel label'}
          confirm={confirmModal}
          confirmLabel={'confirm label'}
        />
      }
    </>
  );
}
```