---
title: Button
sidebar_position: 1
---

# CustomButton

```jsx live
function ButtonShowcase() {
  function clickAlert() {
    window.alert('Button has been clicked!')
  }

  return (
    <CustomButton label={'label'} type={'button'} onClick={() => clickAlert()} />
  );
}
```